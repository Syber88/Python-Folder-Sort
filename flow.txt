in future, allow the program to use the command line arguments to specify which folder to sort
find a way to confirm that a particular extension already exists and then create a folder for it if True
add a condition where if the files from both the subdirectory and the main directory no longer have that extension -- the subdirectory should be deleted
 
